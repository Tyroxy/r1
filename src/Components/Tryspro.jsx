function Tryspro({one, two, three}) {
    return (
<div>
            <h1 style={{color: three}}>{one}</h1>

            <h2 style={{color: three}}>{two}</h2>
        </div>
    )
}

export default Tryspro;