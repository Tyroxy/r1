function Dupro(props) {

    return (
        <div>
            <h1 style={{color:'darkviolet'}}>{props.txt1}</h1>

            <h2 style={{color:'violet'}}>{props.txt2}</h2>
        </div>
    )
}

export default Dupro;